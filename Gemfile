gem 'rspec'        # test suite
gem 'rack-test'    # for making get requests without needing the server running
gem 'rest-client'  # so the client can make real requests
gem 'sinatra'      # the web framework
gem 'sqlite3'      # store user data in a sqlite database
gem 'activerecord' # to talk to the database
